export const serverPort: string = "5000";
export const serverIP: string = "localhost";
export const userAPI: string = `http://${serverIP}:${serverPort}/user`;
export const messageAPI: string = `http://${serverIP}:${serverPort}/message`;